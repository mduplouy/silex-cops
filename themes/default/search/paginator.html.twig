{% set previousPage = '' %}
{% set nextPage = '' %}

{% if pageCount > 1 %}

    {% if pageNum > 1 %}
        {% set previousPage %}
        {% set prevUrl = path('search_results', {'page': pageNum-1, 'keywords': app.request.get('keywords') }) %}
        <a href="{{ prevUrl }}" title="{{ "Previous"|trans|escape }}" class="inline-block ui-corner-all ui-icon ui-icon-triangle-1-w">{{ "Previous"|trans }}</a>
        {% endset %}
    {% endif %}

    {% if pageNum < pageCount %}
        {% set nextPage %}
        {% set nextUrl = path('search_results', {'page': pageNum+1, 'keywords': app.request.get('keywords') }) %}
        <a href="{{ nextUrl }}" title="{{ "Next"|trans|escape }}" class="inline-block ui-corner-all ui-icon ui-icon-triangle-1-e">{{ "Next"|trans }}</a>
        {% endset %}
    {% endif %}

<div class="paginator">
    {{ previousPage }}
    {{ "Page %d on %d (%d results)"|trans|format(pageNum, pageCount, totalRows) }}
    {{ nextPage }}
</div>

{% endif %}

